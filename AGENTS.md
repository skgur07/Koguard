# Koguard 에이전트 개발 가이드

이 파일은 Koguard 저장소에서 작업하는 코딩 에이전트의 기본 규칙이다. Codex의 저장소 탐색,
소유 영역, 변경 전달 형식은 [docs/CODEX-NAVIGATION-GUIDE.md](docs/CODEX-NAVIGATION-GUIDE.md)를
함께 따른다. 하위 디렉터리에 더 구체적인 `AGENTS.md`가 있으면 해당 범위에서는 하위 규칙을
우선한다.

## 프로젝트 기준

- 제품명과 배포명은 `Koguard`/`koguard`를 사용한다.
- 개발 기준 런타임은 CPython 3.11.9이며 패키지 메타데이터는 `>=3.11,<3.12`를 유지한다.
- 패키지와 가상 환경은 `uv`로 관리한다.
- 라이브러리 코드는 `src/koguard`, 테스트는 `tests`에 둔다.
- 현재 공개 범위는 사전 기반 Exact Match와 구간 단위 Whitelist 처리다.
- 외부 사전·모델은 라이선스와 재배포 조건이 확인되기 전까지 배포물에 포함하지 않는다.

## 작업 흐름

1. 요청과 현재 코드의 차이를 확인하고, 복잡한 작업은 짧은 구현 계획을 세운다.
2. 동작 변경에는 실패하는 테스트를 먼저 추가하거나 기존 테스트로 실패를 재현한다.
3. 요청을 충족하는 가장 작은 변경을 구현한다.
4. 포맷, 린트, 타입 검사, 테스트, 빌드를 순서대로 검증한다.
5. 최종 diff를 정확성, 보안, 회귀, 누락된 테스트 관점에서 리뷰한다.
6. 사용자에게 변경 내용, 검증 결과, 남은 위험을 간결하게 보고한다.

세부 절차는 [docs/development-workflow.md](docs/development-workflow.md), 리뷰 기준은
[docs/code-review.md](docs/code-review.md)를 따른다.

## ECC DAILY 스킬

현재 작업이 설명과 일치할 때 다음 스킬을 우선 사용한다.

- `tdd-workflow`: 기능, 버그 수정, 리팩터링
- `error-handling`: 예외 계약이나 실패 경로 변경
- `verification-loop`: 중요한 변경 완료 후 전체 품질 게이트
- `git-workflow`: 브랜치, 커밋, 병합 전략 작업
- `agent-self-evaluation`: 여러 파일에 걸친 작업의 최종 누락 점검

나머지 ECC 스킬은 삭제 대상이 아니라 필요할 때만 찾는 LIBRARY다. 분류 근거는
[docs/ecc-install-plan.md](docs/ecc-install-plan.md)에 기록한다.

## 구현 원칙

- 공개 모델과 설정은 가능한 한 불변으로 유지한다.
- 동일한 입력과 설정에는 항상 동일한 결과와 매치 순서를 반환한다.
- 정규화 결과가 원문 위치를 잃지 않도록 인덱스 매핑을 보존한다.
- Whitelist는 결과 전체가 아닌 겹치는 매치 구간만 보호한다.
- 라이브러리 런타임은 네트워크에 의존하지 않는다.
- 입력 원문이나 탐지된 표현을 기본 로그에 남기지 않는다.
- 최대 입력 길이와 계산량 제한을 우회하는 경로를 만들지 않는다.
- 캐시를 추가할 때는 정확성, 메모리 상한, 동시성 정책이 먼저 정의되어야 한다.
- 불필요한 의존성이나 프레임워크 결합을 core 패키지에 추가하지 않는다.

## 테스트 기준

- 버그 수정에는 재현 테스트를 포함한다.
- 정상 문장, 다중 매치, Whitelist, Unicode, 경계값을 함께 검토한다.
- 테스트는 구현 세부사항보다 공개 동작을 검증한다.
- 전체 branch coverage 기준은 `pyproject.toml`의 90% 이상을 유지한다.
- 성능 관련 변경은 정확도 corpus와 대표 입력 길이의 회귀를 함께 확인한다.

## 검증 명령

```powershell
uv sync --all-extras --dev
uv run ruff format --check .
uv run ruff check .
uv run mypy
uv run pytest
uv build
```

변경 범위가 작아 일부 명령만 실행했다면 실행하지 않은 항목과 이유를 결과에 명시한다.

## Git 규칙

- Git Flow 기준으로 `feature/*`에서 작업하고 `dev`에 반영한 뒤 배포 시 `main`으로 승격한다.
- Conventional Commit 형식을 사용하고 설명은 한국어로 작성한다.
  - 예: `feat: Trie 기반 탐지 단계 추가`
  - 예: `fix: Whitelist 겹침 판정 오류 수정`
  - 예: `docs: 개발 워크플로 문서화`
- 서로 관련 없는 변경을 한 커밋에 섞지 않는다.
- push, merge, PR 생성처럼 외부 상태를 바꾸는 작업은 사용자가 요청한 범위에서만 수행한다.

## 외부 작업 경계

- 네트워크 도구는 기본적으로 검색과 조회에만 사용한다.
- 게시, push, merge, 유료 작업, 원격 에이전트 실행, 외부 리소스 및 자격 증명 변경에는
  명시적인 사용자 승인이 필요하다.
- 비밀정보를 코드, 문서, 로그, 설정에 하드코딩하지 않는다.

## 에이전트 역할

프로젝트의 `.codex/agents`에는 읽기 전용 탐색, 리뷰, 문서 조사 역할이 정의되어 있다.
서브에이전트 사용은 사용자가 명시적으로 요청했거나 상위 실행 지침이 허용할 때만 한다.

- `koguard_explorer`: 실행 경로와 영향 범위를 조사한다.
- `koguard_reviewer`: 정확성, 보안, 회귀, 테스트 누락을 리뷰한다.
- `koguard_docs_researcher`: 공식 문서와 1차 자료로 변경 가능성이 있는 사실을 검증한다.

## 완료 조건

- 요청한 동작과 문서가 일치한다.
- 관련 테스트를 추가하거나 변경할 필요가 없는 이유가 분명하다.
- 적용 가능한 검증 명령이 통과한다.
- 디버그 코드, 임시 파일, 비밀정보가 남아 있지 않다.
- 알려진 제한과 후속 작업이 사용자에게 전달된다.
